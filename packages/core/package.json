{
  "name": "taro-ui",
  "version": "0.0.1",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@taro-ui/theme": "workspace:*",
    "@taro-ui/components": "workspace:*"
  }
}
